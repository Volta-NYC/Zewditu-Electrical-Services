# Destination for contact form submissions.
# Any endpoint that accepts a multipart POST works: Formspree, Basin, Getform,
# or a Vercel serverless function in this project.
#
# While this is unset, the form validates normally and then tells the visitor
# to call or email instead of pretending the message was delivered.
NEXT_PUBLIC_CONTACT_ENDPOINT=
